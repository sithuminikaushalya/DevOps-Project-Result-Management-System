apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: result-management
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: kaushalyasithumini29/frontend:latest
        ports:
        - containerPort: 3000
